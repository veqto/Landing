DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require
DIRECT_URL=postgresql://user:pass@host/dbname?sslmode=require
NEXT_PUBLIC_APP_URL=https://veqto.co
NEXT_PUBLIC_APP_NAME=Veqto

# Base de la plataforma que recibe las solicitudes de crédito.
# Sin esta variable el formulario no envía nada y muestra un error explícito
# en vez de un falso "solicitud recibida". Ver src/lib/credit-application-api.ts
NEXT_PUBLIC_VEQTO_API_BASE_URL=https://app.veqto.ai
